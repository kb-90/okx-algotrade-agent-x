YOUR_API_KEY=add_yours_here
YOUR_SECRET_KEY=add_yours_here
YOUR_PASSPHRASE=add_yours_here
OK-ACCESS-KEY=YOUR_API_KEY # Don't change this!
OK-ACCESS-PASSPHRASE=YOUR_PASSPHRASE # Don't change this!
# OK-ACCESS-SIGN is generated dynamically in the code
# OK-ACCESS-TIMESTAMP is generated dynamically in the code
flag=1
DEMO_MODE=1
# "1" uses OKX Demo Trading with header x-simulated-trading:1
# "0" removes the header and trades LIVE

# Other optional OKX settings
OKX_WS_PUBLIC_URL=wss://ws.okx.com:8443/ws/v5/public
OKX_WS_PRIVATE_URL=wss://ws.okx.com:8443/ws/v5/private
OKX_BASE_URL=https://www.okx.com

# You can over-ride the evosearch or config.py parameter settings by adding the parameters here below, then calling them with os.getenv() checks in config.py:
# exit_mode=intelligent
# future_bars=

